# Remove any iptables rules added
# Rules based on conflicting bridge interfaces could take precedence over new bridge interface

$ minikube delete
$ minikube delete --all
$ minikube delete --purge
$ docker rmi gcr.io/k8s-minikube/kicbase:v0.0.36
